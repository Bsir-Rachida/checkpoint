{% extends 'base.html.twig' %}

{% block title %}Contact
{% endblock %}

{% block body %}

	<div class="container">
		<h1 class="project text-center mt-4">
			CONTACT</h1>
		

		<hr class="ms-5 w-80">
		<h3 class="text-center text-secondary mt-4">
			POUR PLUS D’INFOS N’HESITEZ PAS À ME CONTACTER</h3>


		<div class="  mt-3 text-start fs-4">
			<h3>{{ form_start(form) }}

				{{ form_widget(form) }}</h3>

				<button class="btn btn-primary ">Envoyer</button>

				{{ form_end(form) }}
		</div>
	</div>

{% endblock %}
